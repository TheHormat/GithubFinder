import React, { Component } from "react";

export class Navbar extends Component {
  render() {
    return (
      <nav className="navbar navbar-dark bg-primary">
        <a href="/" className="navbar-brand">
          <i className="bi bi-github"></i>sdsd
        </a>
      </nav>
    );
  }
}

export default Navbar;
